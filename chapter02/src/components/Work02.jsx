import { Button, Box, Stack, Typography, Divider } from '@mui/material';
import { styled } from '@mui/material/styles';

export const Work02 = () => (
  <Stack>
    <Typography variant="h5" sx={{color: 'red'}}>Hello</Typography>
    <Typography variant="h5" sx={{fontSize: 20}}>Hello</Typography>
    <Typography variant="h5" sx={{fontStyle: 'italic'}}>Hello</Typography>
    <Typography variant="h5" sx={{textDecoration: 'line-through'}}>Hello</Typography>
    <Typography variant="h5" sx={{textDecoration: 'underline'}}>Hello</Typography>
  </Stack>
);
